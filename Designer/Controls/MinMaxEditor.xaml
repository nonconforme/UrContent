<UserControl x:Class="Designer.Controls.MinMaxEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Label Content="Min" Grid.Column="0" Margin="2"/>
        <TextBox Name="minVal" Text="{Binding Min}" Grid.Column="1" MinWidth="90"/>
        <Label Content="Max" Grid.Column="2" Margin="2"/>
        <TextBox Name="maxVal" Text="{Binding Max}" Grid.Column="3" MinWidth="90"/>
    </Grid>
</UserControl>
